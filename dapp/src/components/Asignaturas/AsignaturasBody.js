
/*import AsignaturaRow from "./AsignaturaRow";

const AsignaturasBody = (props) => {
    const {drizzle, drizzleState, AsignaturasLength} = props;
    let rows = [];
    for (let i = 0; i < asignaturasLength; i++) {
        rows.push(<AsignaturaRow drizzle={drizzle}
                                 drizzleState={drizzleState}
                                 asignaturaIndex={i}/>);
    }
    return <tbody>{rows}</tbody>;
};

export default AsignaturasBody;*/
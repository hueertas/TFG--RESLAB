/*import {newContextComponents} from "@drizzle/react-components";

const {ContractData} = newContextComponents;

const AsignaturaRow = (props) => {
    const {drizzle, drizzleState, evaluacionIndex} = props;
    return <ContractData
        drizzle={drizzle}
        drizzleState={drizzleState}
        contract={"reslabetsit"}
        method={"asignaturas"}
        methodArgs={[asignaturaIndex]}
        render={AS =>
            <tr key={"ASIG-" + asignaturaIndex}>
                <th>E<sub>{asignaturaIndex}</sub></th>
                <td>{AS.laboratorio}</td>
                
            </tr>
        }
    />;
};

export default asignaturaRow;*/